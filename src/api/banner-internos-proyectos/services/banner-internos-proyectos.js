'use strict';

/**
 * banner-internos-proyectos service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::banner-internos-proyectos.banner-internos-proyectos');
