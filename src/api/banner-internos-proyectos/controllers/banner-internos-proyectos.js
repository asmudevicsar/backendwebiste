'use strict';

/**
 *  banner-internos-proyectos controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::banner-internos-proyectos.banner-internos-proyectos');
