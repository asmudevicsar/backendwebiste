'use strict';

/**
 * banner-internos-proyectos router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::banner-internos-proyectos.banner-internos-proyectos');
