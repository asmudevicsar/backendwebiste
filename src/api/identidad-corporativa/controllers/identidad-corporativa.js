'use strict';

/**
 *  identidad-corporativa controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::identidad-corporativa.identidad-corporativa');
