'use strict';

/**
 * identidad-corporativa service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::identidad-corporativa.identidad-corporativa');
