'use strict';

/**
 * categoria-de-evento-noticia service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::categoria-de-evento-noticia.categoria-de-evento-noticia');
