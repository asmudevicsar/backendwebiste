'use strict';

/**
 * identidad-organizativa service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::identidad-organizativa.identidad-organizativa');
