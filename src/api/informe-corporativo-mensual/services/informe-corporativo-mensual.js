'use strict';

/**
 * informe-corporativo-mensual service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::informe-corporativo-mensual.informe-corporativo-mensual');
