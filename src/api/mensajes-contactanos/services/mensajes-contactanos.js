'use strict';

/**
 * mensajes-contactanos service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::mensajes-contactanos.mensajes-contactanos');
